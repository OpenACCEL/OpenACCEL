require.config({
    baseUrl: "scripts"
});

define(["lodash"], /**@lends View*/ function(_) {
    /**
     * @class
     * @classdesc The introduction tab.
     */
    function IntroTab() {

    }

    return IntroTab;
});
